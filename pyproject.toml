[project]
name = "laptop-service-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "moddheswar", email = "smoddheswar@mgail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.141.1",
    "google-genai>=2.24.0",
    "pytest>=9.1.1",
    "uvicorn>=0.53.0",
]

[project.scripts]
laptop-service-agent = "laptop_service_agent:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
